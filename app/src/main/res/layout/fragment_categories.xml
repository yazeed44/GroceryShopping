<GridView xmlns:android="http://schemas.android.com/apk/res/android"
    android:id="@+id/categories_list"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    android:clipToPadding="false"
    android:columnWidth="@dimen/category_width"
    android:horizontalSpacing="@dimen/category_horizontal_spacing"
    android:numColumns="@integer/categories_column_num"
    android:scrollbarStyle="outsideOverlay"

    android:verticalSpacing="@dimen/category_vertical_spacing" />